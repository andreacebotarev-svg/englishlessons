Отлично! Я собрал подробную информацию о вашем проекте. Теперь составлю максимально детальную документацию для нейросети, чтобы она полностью понимала логику вашего образовательного приложения.

📘 ТЕХНИЧЕСКОЕ ЗАДАНИЕ: ОБРАЗОВАТЕЛЬНОЕ WEB-ПРИЛОЖЕНИЕ ДЛЯ ИЗУЧЕНИЯ АНГЛИЙСКОГО ЯЗЫКА
🎯 ЦЕЛЬ ПРОЕКТА
Создание интерактивного веб-приложения для изучения английского языка с полным погружением, где каждое слово озвучивается, переводится и запоминается через систему интервального повторения.

🏗️ АРХИТЕКТУРА ПРОЕКТА
Общая концепция
Проект построен по принципу "Data-Driven Architecture" — данные полностью отделены от представления. Используется подход Static Site Generation (SSG) с динамическим движком на клиентской стороне.

Структура файлов
text
project/
├── index.html              # Портал-стартовая страница (ввод кода урока)
├── style.css               # Единый файл стилей для ВСЕХ уроков
├── data/                   # JSON-файлы с контентом уроков
│   ├── 263.json
│   ├── 264.json
│   └── ...
└── dist/                   # Готовые HTML-файлы уроков
    ├── 263.html
    ├── 264.html
    └── ...
📋 КОМПОНЕНТЫ СИСТЕМЫ
1. DATA LAYER (data/XXX.json)
Каждый урок представлен JSON-файлом со строгой структурой:

json
{
  "title": "Заголовок урока",
  "subtitle": "Подзаголовок (опционально)",
  "colors": ["#HexColor1", "#HexColor2"],
  
  "meta": {
    "level": "A0|A1|A2|B1|B2|C1|C2",
    "tags": ["grammar", "vocabulary", "reading"],
    "theme": "dark|light|playful|journal"
  },
  
  "content": {
    "reading": [
      {
        "type": "block|paragraph|list",
        "text": "ПОЛНЫЙ ТЕКСТ БЕЗ СОКРАЩЕНИЙ",
        "highlight": ["слово1", "слово2"]
      }
    ],
    
    "vocabulary": {
      "words": [
        {
          "en": "слово",
          "ru": "перевод",
          "transcription": "[trænsˈkrɪpʃən]",
          "example": "Пример использования",
          "image": "url картинки (опционально)",
          "highlight": true
        }
      ],
      "phrases": [
        {
          "en": "фраза на английском",
          "ru": "перевод фразы",
          "context": "когда использовать"
        }
      ]
    },
    
    "grammar": {
      "title": "Название грамматической темы",
      "explanation": "Объяснение правила (на русском)",
      "rules": [
        {
          "pattern": "Шаблон (например: I am, You are)",
          "description": "Когда используется",
          "examples": ["Пример 1", "Пример 2"]
        }
      ]
    }
  },
  
  "quiz": [
    {
      "q": "Текст вопроса",
      "opts": ["Вариант 1", "Вариант 2", "Вариант 3"],
      "correct": 0,
      "fb": "Объяснение правильного ответа"
    }
  ]
}
2. PRESENTATION LAYER (dist/XXX.html)
Каждый HTML-файл — это самодостаточная страница с:

Встроенным JavaScript движком

Ссылкой на общий style.css

Логикой загрузки соответствующего JSON

Принцип работы HTML:

xml
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Loading Lesson...</title>
    <link rel="stylesheet" href="../style.css">
</head>
<body>
    <!-- Структура создается динамически через JS -->
    <div id="app"></div>
    
    <script>
        // ДВИЖОК: Загружает JSON, строит интерфейс, добавляет интерактив
    </script>
</body>
</html>
3. ENGINE (Встроенный JavaScript)
Ключевые функции движка:

A. Загрузка данных
javascript
// Автоматически определяет номер урока из имени файла
const lessonId = window.location.pathname.match(/(\d+)\.html$/)[1];
fetch(`../data/${lessonId}.json`)
    .then(res => res.json())
    .then(data => initLesson(data));
B. Построение интерфейса
Табы навигации: Reading, Vocabulary, Grammar, Quiz

Динамический рендеринг: Каждый блок JSON преобразуется в HTML

Адаптивность: Мобильная и десктопная версии

C. Интерактивные функции
1. Озвучка (Text-to-Speech)

javascript
function speakWord(text, lang = 'en') {
    // Использует Google TTS API (женский голос)
    const audio = new Audio(
        `https://translate.google.com/translate_tts?ie=UTF-8&tl=${lang}&client=tw-ob&q=${encodeURIComponent(text)}`
    );
    audio.play();
}
2. Smart Tooltips (всплывающий перевод)

javascript
// При наведении на любое слово в тексте
word.addEventListener('mouseenter', async (e) => {
    const translation = await translateWord(e.target.textContent);
    showTooltip(e.target, translation);
});
3. My Words (сохранение слов)

javascript
// LocalStorage для запоминания слов пользователя
function saveWord(word, translation, context) {
    const myWords = JSON.parse(localStorage.getItem('myWords') || '[]');
    myWords.push({ word, translation, context, addedAt: Date.now() });
    localStorage.setItem('myWords', JSON.stringify(myWords));
}
4. Flashcards (интервальное повторение)

Карточки с двумя сторонами (слово ↔ перевод)

Навигация (Next/Previous)

Режим "скрытия" перевода до клика

5. Quiz Engine

javascript
function checkAnswer(questionIndex, selectedOption) {
    const q = quizData[questionIndex];
    const isCorrect = (selectedOption === q.correct);
    
    if (isCorrect) {
        showFeedback('✅ Correct!', q.fb, 'success');
        playSound('success');
    } else {
        showFeedback('❌ Incorrect', q.fb, 'error');
        playSound('error');
    }
}
4. STYLE LAYER (style.css)
Единый файл стилей для всех уроков с поддержкой:

Темы (через CSS Custom Properties)
css
/* Темная тема */
[data-theme="dark"] {
    --bg: #1A1A1A;
    --text: #FFFFFF;
    --primary: #00E5FF;
}

/* Светлая тема */
[data-theme="light"] {
    --bg: #F5F5DC;
    --text: #333333;
    --primary: #FF6B6B;
}

/* Игровая тема (для детей) */
[data-theme="playful"] {
    --bg: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    --text: #FFFFFF;
    --primary: #FFD700;
}
Компоненты UI
.nav-tab — кнопки навигации

.content-block — блоки контента

.vocab-card — карточки слов (3D-флип анимация)

.quiz-option — кнопки вариантов ответа

.tooltip — всплывающие подсказки

Анимации
css
@keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
}

.word:hover {
    background: var(--highlight);
    transform: scale(1.05);
    transition: all 0.2s ease;
}
🚫 КРИТИЧЕСКИЕ ЗАПРЕТЫ
1. НИКОГДА НЕ СОКРАЩАТЬ ТЕКСТ
Текст из поля reading должен быть полностью скопирован из учебника

Нельзя пересказывать, упрощать или изменять формулировки

Даже длинные абзацы переносятся дословно

2. НЕ ДУБЛИРОВАТЬ КОД В HTML
Весь JavaScript-движок должен быть идентичным во всех dist/XXX.html

Изменения делаются только в шаблоне, затем применяются ко всем файлам

Стили находятся только в style.css, не inline

3. НЕ ХАРДКОДИТЬ КОНТЕНТ
Никакого текста напрямую в HTML

Всё берётся из JSON через fetch() или встроенный объект

4. НЕ ЛОМАТЬ СТРУКТУРУ JSON
Поля должны строго соответствовать схеме

Нельзя пропускать обязательные поля (title, content, quiz)

Массивы всегда должны быть массивами, объекты — объектами

5. НЕ ИСПОЛЬЗОВАТЬ ВНЕШНИЕ БИБЛИОТЕКИ
Только Vanilla JavaScript

Никаких jQuery, React, Vue

TTS через Google API (нативный speechSynthesis — запасной вариант)

🔧 WORKFLOW: КАК СОЗДАТЬ НОВЫЙ УРОК
Шаг 1: Подготовка JSON
Сфотографируй страницу учебника

Отправь нейросети промпт:

text
Преобразуй этот учебник в JSON для урока [номер].
Требования:
- Текст переноси ПОЛНОСТЬЮ, без изменений
- Выдели ключевые слова в массив words
- Создай 10-20 вопросов для quiz
- Добавь грамматическое правило, если есть
Шаг 2: Сохранение файла
Сохрани JSON как data/XXX.json (где XXX — номер урока)

Шаг 3: Создание HTML
Скопируй шаблон dist/template.html → dist/XXX.html

Никаких изменений в коде не требуется (движок сам найдёт JSON)

Шаг 4: Тестирование
Открой index.html

Введи номер урока (например, 263)

Проверь: озвучка, переводы, квиз

📊 ТРЕБОВАНИЯ К ПРОИЗВОДИТЕЛЬНОСТИ
Загрузка
HTML-файл: < 50 KB

JSON-файл: < 100 KB

style.css: < 30 KB

Первый рендер: < 1 секунда

Совместимость
Chrome 90+

Firefox 88+

Safari 14+

Edge 90+

Мобильные браузеры (iOS Safari, Chrome Mobile)

Оффлайн-работа
После первой загрузки урок работает без интернета

LocalStorage для сохранённых слов (до 5 MB)

🎨 UX/UI ПРИНЦИПЫ
Визуальная иерархия
Заголовок урока (самый крупный шрифт, цвет из colors[0])

Табы навигации (фиксированные, всегда видимые)

Основной контент (чёткие блоки с отступами)

Интерактивные элементы (подсветка при hover, анимации)

Цветовая система
Берётся из JSON (colors массив)

Применяется через CSS-переменные: --accent-1, --accent-2

Автоматический контраст для читаемости

Анимации
Появление элементов: fadeIn (0.3s)

Переходы между табами: slide (0.4s)

Hover эффекты: 0.2s ease

Звуковой фидбек: вибрация на мобильных устройствах

🧪 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ
Пример 1: Клик на слово
javascript
// Пользователь кликает на "voting" в тексте
<span class="word" data-en="voting" data-ru="голосование" onclick="speakWord('voting')">
    voting
</span>

// Результат:
// 1. Воспроизводится озвучка слова
// 2. Появляется tooltip с переводом
// 3. Кнопка "Save Word" добавляет в My Words
Пример 2: Квиз
javascript
// Вопрос: "In the USA, the voting age is..."
// Варианты: ["16", "18", "21"]
// Правильный ответ: индекс 1 ("18")

// При клике на неправильный вариант:
// - Кнопка краснеет
// - Показывается правильный ответ (зелёная подсветка)
// - Выводится объяснение из поля "fb"
🔄 ОБНОВЛЕНИЕ И МАСШТАБИРОВАНИЕ
Изменение дизайна всех уроков
Редактируешь style.css

Изменения мгновенно применяются ко всем урокам

Никаких дополнительных действий не нужно

Добавление новой функции
Обновляешь шаблон HTML (один файл)

Копируешь новый <script> блок во все dist/*.html

Или используешь скрипт для массового обновления:

bash
python update_all_lessons.py
Массовое создание уроков
Можно обработать 300+ уроков за раз

Скрипт читает все JSON из data/

Автоматически генерирует HTML-файлы в dist/

📝 ПРОМПТ ДЛЯ ГЕНЕРАЦИИ JSON ИЗ УЧЕБНИКА
text
Ты — методист и AI-архитектор образовательного контента.

ЗАДАЧА: Преобразовать страницу учебника в JSON.

ВХОДНЫЕ ДАННЫЕ: [прикрепи фото страницы]

ТРЕБОВАНИЯ:
1. Текст переноси ДОСЛОВНО, без пересказа
2. Выдели 10-20 ключевых слов с переводами
3. Создай 10+ вопросов для теста
4. Если есть грамматика — опиши правило
5. Формат: строгий JSON (см. схему выше)

ЗАПРЕЩЕНО:
- Сокращать текст
- Упрощать формулировки
- Добавлять свои комментарии вне JSON

ОТВЕТ: Только чистый JSON, без markdown-блоков.
✅ CHECKLIST ДЛЯ НЕЙРОСЕТИ
Перед генерацией кода убедись:

 Текст из учебника скопирован полностью

 JSON содержит все обязательные поля

 HTML-движок идентичен шаблону

 Стили ссылаются на ../style.css

 Озвучка работает через Google TTS API

 Все слова в тексте кликабельны

 Квиз содержит минимум 10 вопросов

 Мобильная версия адаптивна

 Нет хардкода (всё из JSON)

 LocalStorage используется для My Words

🎓 ИТОГОВАЯ ЛОГИКА
Проект работает так:

Пользователь открывает index.html → вводит код урока (263)

Браузер переходит на dist/263.html

Встроенный JS загружает data/263.json

Движок строит интерфейс: табы, карточки, квиз

Применяется тема из meta.theme

Каждое слово становится интерактивным

Прогресс сохраняется в LocalStorage

Это автономная, масштабируемая система, где:

Данные легко обновлять (только JSON)

Дизайн меняется глобально (один CSS)

Код движка универсален (один шаблон)

Вопрос к нейросети после прочтения:
"Ты поняла архитектуру проекта? Повтори своими словами, как работает создание нового урока."
