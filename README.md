# 🏰 englishlessons - Palace Engine with Cinematic Camera

Интерактивные уроки английского с 3D Memory Palace и cinematic камерой в стиле Portal 2.

---

## 🧠 Memory Palace + картинки

Palace Engine теперь использует чистый Three.js рендеринг с кинематографической камерой. Система работает так:

1. `data/263.json` содержит слова в `content.vocabulary.words`.
2. Скрипт `scripts/generate-lesson-images.js`:
   - скачивает изображения с Pexels
   - складывает в `images/263/`
   - прописывает в JSON поле `"image": "263/vote.jpg"`.
3. `palace_engine/js/builder-threejs.js` берёт `word.image` и создаёт Three.js меш:
   ```javascript
   // Создание текстурированной карточки с изображением
   const card = createWordCard(word, positionZ);
   card.userData = { word: word.word, translation: word.translation, image: word.image };
   ```
4. Система отображения карточек теперь использует Three.js материалы и шейдеры.

Подробнее про скрипты: [`scripts/README.md`](scripts/README.md).

---

## 🚀 Генерация картинок локально

```bash
# 1. Устанавливаешь Node.js
# 2. В корне репозитория:

# базовый запуск
node scripts/generate-lesson-images.js 263

# с API-ключом через переменную
PEXELS_API_KEY="your_key_here" node scripts/generate-lesson-images.js 263
```

После запуска получаешь:
- папку `images/263/` с картинками
- обновлённый `data/263.json` с полем `image` у каждого слова.

Открываешь `palace_engine/index.html?lesson=263` — в коридоре должны появиться карточки с картинками.

---

## 🔁 GitHub Actions (опционально)

В репозитории есть workflow: [`.github/workflows/generate-images.yml`](.github/workflows/generate-images.yml).

Что делает:
- по кнопке **Run workflow** в GitHub Actions
- просит ввести номер урока
- запускает `node scripts/generate-lesson-images.js <lesson>` на сервере
- коммитит изменения в `data/` и `images/`.

Нужно только добавить секрет `PEXELS_API_KEY` в настройках репозитория.

---

## 🧩 Что уже готово

- `builder-threejs.js` рендерит 3D комнаты с текстурированными карточками и цветами по сложности  
- `CinematicCamera.js` управляет кинематографической камерой  
- `CameraControls.js` обеспечивает плавное управление  
- `scripts/generate-lesson-images.js` автоматизирует создание `image` для слов  

Дальше можно просто добавлять новые уроки в `data/XXX.json` и запускать генератор.

---

## 🎬 Cinematic Camera System (Portal 2 Style)

**НОВИНКА: Palace Engine теперь включает полнофункциональную cinematic камеру в стиле Portal 2!**

### Основные возможности:
- **Rail-based Movement**: Камера движется по плавным кривым (Catmull-Rom splines)
- **Cinematic FOV**: Узкий угол обзора (35°) для кинематографического эффекта
- **Auto-focus**: Автоматическое фокусирование на карточках
- **Smooth Animations**: Плавные переходы с использованием GSAP
- **Quaternion Rotation**: Плавные вращения без gimbal lock
- **Interactive Controls**: Управление клавиатурой и мышью

### Управление:
- **W/S или ↑/↓**: Движение вперед/назад
- **Space**: Переход к следующей карточке
- **Home/End**: Переход к началу/концу коридора
- **Escape**: Возврат к режиму рельсов
- **R**: Показать/скрыть рельсы
- **Клик мышью**: Фокус на карточке

### Технологии:
- **Three.js**: 3D рендеринг и математика
- **GSAP**: Плавные анимации
- **gl-matrix**: Математические вычисления
- **Catmull-Rom Splines**: Плавные траектории

### Файлы системы:
- `palace_engine/CinematicCamera.js` - Основная логика камеры
- `palace_engine/CameraControls.js` - Обработка управления
- `palace_engine/js/builder-threejs.js` - 3D генерация мира
- `palace_engine/example_integration.js` - Примеры интеграции
- `palace_engine_demo.html` - Демонстрация системы

### Пример запуска:
Откройте `palace_engine_demo.html` в браузере, чтобы увидеть cinematic камеру в действии с вашими карточками!

---

## 🔄 Миграция на чистый Three.js

Palace Engine был успешно мигрирован с гибридной системы (CSS 3D + Three.js) на чистый Three.js рендеринг:

- Удалена система на базе `builder.js` и CSS 3D трансформаций
- Всё приложение теперь использует Three.js для 3D рендеринга
- Сохранена вся функциональность cinematic камеры
- Улучшена производительность и стабильность
- Упрощена архитектура за счёт единого рендеринга
