🏛 Архитектурный Манифест v4.0: JSON-Driven Static Templates
1. Концепция "Умного Шаблона" (The Smart Template Concept)
Мы перешли от традиционной серверной генерации к архитектуре Client-Side Rendering (CSR) на основе статических файлов.

Разделение ответственности:

Контент (/data/*.json): Единый источник правды. Содержит только данные (текст, настройки, цвета), без верстки.

Оболочка (/dist/*.html): "Пустой" HTML-каркас. Он не содержит контента урока, но содержит логику (engine.js + style.css), которая знает, как превратить JSON в красивую веб-страницу.

Движок (engine.js): "Строитель", который в момент загрузки страницы соединяет JSON с HTML-шаблонами.

Механизм работы (Runtime Magic):

Пользователь открывает dist/263.html.

Встроенный скрипт определяет ID урока из имени файла (263).

Скрипт делает асинхронный запрос (fetch) к файлу ../data/263.json.

Получив данные, скрипт генерирует DOM-элементы (заголовки, карточки слов, тесты) и вставляет их в страницу.

Пользователь видит готовый урок. Весь процесс занимает миллисекунды.

Преимущества:

Масштабируемость: Создание нового урока = создание JSON + копирование файла-шаблона.

Поддерживаемость: Изменение дизайна в style.css или логики в engine.js автоматически обновляет все существующие уроки.

AI-Friendly: Нейросетям гораздо проще генерировать структурированный JSON, чем писать валидный HTML с версткой.

2. Структура Проекта (File Structure)
Проект организован максимально плоско и понятно:

text
/project-root
│
├── index.html          # [PORTAL] Точка входа. Красивое меню ввода кода урока.
├── engine.js           # [BUILDER] Главный скрипт. Логика рендеринга и интерактивности.
├── style.css           # [SKIN] Глобальные стили. Темы, сетки, анимации.
│
├── /data               # [DATABASE] Контент уроков (JSON)
│   ├── 263.json        # Данные урока 263
│   ├── 264.json        # Данные урока 264
│   └── ...
│
└── /dist               # [PAGES] HTML-оболочки уроков
    ├── 263.html        # Оболочка для урока 263 (код внутри идентичен 264.html)
    ├── 264.html        # Оболочка для урока 264
    └── ...
3. Инструкция для AI (Workflow создания урока)
Если ты (AI) хочешь создать новый урок (например, ID 265), выполни строго два шага:

Шаг 1: Генерация Контента (JSON)
Создай файл data/265.json. Используй следующую строгую схему.
Важно: Текст внутри content (поле story) должен быть HTML-строкой, но без <html> и <body>, только контент.

json
{
  "title": "Space Exploration",
  "subtitle": "Unit 6: The Final Frontier",
  "colors": ["#3F51B5", "#C5CAE9"],
  "theme": "dark",
  "meta": {
    "level": "B2",
    "tags": ["Space", "Science"],
    "reading_time": "5 min"
  },
  "content": [
    {
      "type": "block",
      "title": "The Beginning",
      "text": "Humanity has always looked at the stars..."
    },
    {
      "type": "vocab",
      "items": [
        { "en": "Launch", "ru": "Запуск" },
        { "en": "Orbit", "ru": "Орбита" }
      ]
    },
    {
      "type": "quiz",
      "question": "When was the first moon landing?",
      "options": ["1969", "1959"],
      "answer": 0,
      "feedback": "Correct! Apollo 11 landed in 1969."
    }
  ]
}
Шаг 2: Генерация Оболочки (HTML)
Создай файл dist/265.html.
Код этого файла ВСЕГДА ОДИНАКОВЫЙ. Не меняй в нем ничего, кроме имени самого файла при сохранении. Вся магия происходит благодаря подключению ../engine.js.

Универсальный код шаблона (dist/XXX.html):

xml
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Loading lesson...</title>
    <link rel="stylesheet" href="../style.css">
</head>
<body>
    <!-- Корневой контейнер. Engine.js наполнит его контентом -->
    <div class="container">
        <div id="app">
            <div style="height: 80vh; display: flex; flex-direction: column; justify-content: center; align-items: center; color: #999; gap: 15px;">
                <div style="width: 40px; height: 40px; border-radius: 50%; background: #ccc; animation: pulse 1.5s infinite;"></div>
                <div>Loading lesson data...</div>
                <style>@keyframes pulse { 0% {opacity:0.3; transform:scale(0.8)} 50% {opacity:0.6; transform:scale(1.2)} 100% {opacity:0.3; transform:scale(0.8)} }</style>
            </div>
        </div>
    </div>

    <!-- ПОДКЛЮЧЕНИЕ ДВИЖКА (Самая важная строка) -->
    <script src="../engine.js"></script>
</body>
</html>
4. Технические Требования к Запуску
Из-за политики безопасности браузеров (CORS), JavaScript не может загружать файлы с диска (протокол file://) через fetch().

Как запустить проект локально:

VS Code (Рекомендуемый): Установи расширение "Live Server". Нажми правой кнопкой на index.html -> "Open with Live Server".

Python: Открой терминал в папке проекта и введи: python -m http.server. Открой в браузере localhost:8000.

Node.js: Если установлен http-server (npm install -g http-server), введи http-server.

В продакшене (GitHub Pages):
Просто залейте код в репозиторий и включите GitHub Pages в настройках. Всё заработает автоматически, так как это уже настоящий веб-сервер.
